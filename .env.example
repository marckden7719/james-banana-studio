# Public env vars exposed to the client (must be prefixed with VITE_)
# VITE_SITE_URL=https://jamesbanana.xyz

# Server-only secrets (do NOT prefix with VITE_) — set in Vercel → Project → Settings → Environment Variables
# EXAMPLE_API_KEY=